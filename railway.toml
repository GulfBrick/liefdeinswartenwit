$schema = "https://railway.com/railway.schema.json"

[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/wedding"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
